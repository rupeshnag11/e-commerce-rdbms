[project]
name = "e-commerce"
version = "0.1.0"
description = ""
authors = [
    {name = "Rupesh"}
]
requires-python = ">=3.14"
dependencies = ["fastapi (>=0.141.1,<0.142.0)"]

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"